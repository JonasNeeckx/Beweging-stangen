function k_optimal = spring(Lift, F_load, F_inert, normalForce, Pressure_Angle)
optimal_N = inf;

for Fv0 = 50:0.01:300
    for k = 1:(size(F_inert,2))
    Springconstant= (-F_load(k) -Fv0 - F_inert(k))/Lift(k);
    N = (F_load(k) + Fv0 + Springconstant*Lift(k) + F_inert(k))/cos(Pressure_Angle(k));
    end
    Nmax=max(N);
    if Nmax < optimal_N
        optimal_N = N;
        Springconstant
        
end